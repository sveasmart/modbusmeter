/**
 * A fake version of rpio_click_detector.
 * Triggers fake click events whenever enter is pressed.
 */
class FakeClickDetector {

  setClickListener(onClickFunction) {
    //TODO listen for 'enter' presses and trigger the onClickFunction
  }
}